# CLAUDE.md

クレーンゲーム型3Dテトリス（Unity 6 / URP）。

**まず `Docs/3DTetris_GameDesign.md` を読むこと。** 仕様・設計はすべてそこにある。このファイルはその要約ではなく、作業時に守る規約だけを書いている。

## 対話

- 日本語で回答する
- 前提が曖昧なら、実装に入る前に質問する
- 堅牢さと拡張性に配慮しつつ、YAGNIで進める。「将来必要かも」で作らない

## 絶対に崩さない設計の約束

1. **`Core` は Unity に依存しない。** MonoBehaviour / Transform / Time / UniTask を持ち込まない。同期的で決定的なままにする。これはEditModeテスト全体の前提
2. **依存は一方向。** `Core` ← `Presentation` / `Input` ← `App`。`Core` は何も参照しない。asmdefで強制済み
3. **座標は2種類。** セル座標（`Vector3Int`、ロジック）とワールド座標（`Vector3`、描画）。変換はセル → ワールドの一方向のみ。逆算は作らない
4. **回転にfloatを混ぜない。** 姿勢は整数の3×3行列。`Quaternion` は見た目の補間だけに使う
5. **回転してもピースは盤面上で動かない。** 姿勢テーブルは正規化されているので、回転時に origin を補正する（仕様書 5.3）
6. **盤面配列は `board[x, y, z]` で統一。** 順序を混ぜない
7. **演出を0秒にしてもゲームが同じ結果で進行すること。** ロジックは演出の完了を待たない
8. **`Core` に `UnityEngine` の型を入れない。** 特に `Color` / `Material`。色は `Presentation` の `PieceVisualTable` が持つ

## 実装の規約

- **`GameSession` と `GameDirector` の接続は命令メソッド＋戻り値。** C# `event` は使わない（購読者が1人しかいない。根拠は仕様書 11.4）
- `GameRuleAsset` は `OnValidate` で不変条件を検証する。特に `boardSize.y >= deadLineY + 4`
- 非同期は UniTask。`Presentation` / `App` / `Input` のみ
- `async void` を書かない。戻り値不要なら `UniTaskVoid` + `.Forget()`
- 非同期メソッドには `CancellationToken` を必ず引数に取る
- R3 / UniRx は入れない（判断の根拠は仕様書 11.5）
- ブロックの描画はオブジェクトプール。`Instantiate` / `Destroy` を毎回呼ばない
- 調整用の数値はコードに直接書かず `GameRuleAsset` / `PieceShapeAsset` に置く
- 画面は Title / InGame / Paused / Result の4つ。**シーンは1つだけ**で、切り替えはUIのオン・オフ。`SceneManager` でシーンを分けない
- リトライは `GameSession` を作り直す。前回の状態をリセットする処理は書かない
- **入力はワールド固定。** 矢印キーの向きはカメラの角度に影響されない。`PlayerInputRouter` から `OrbitCameraRig` を参照しない
- 視点操作（右ドラッグ）は `GameSession` に通さず、`Presentation` 層で完結させる。ドロップ演出中もポーズ中も動かせる
- 制限時間・手番タイマー・自動落下は**入れない**。難易度を時間で作らない
- 盤面は高さ12段（`Y=0..7` がプレイ領域、`8..11` は終了判定用バッファ）。バッファ層は描画しない

## 画面に何かを足すとき

**足すという行為そのものがプレイヤーの負荷を高めている。** 追加してよいのは、それが減らす負荷が、追加によって増える負荷を明確に上回ると確認できた場合だけ。「あると便利」は理由にならない。すべてを示すことが親切なのではなく、一部だけを示して済ませ、それ以上の追加を止めるのが設計の仕事。

- **補助表示は影だけ。** ゴースト、壁の帯、レイヤー充填ゲージ、方位コンパスは入れない（判断の根拠は仕様書 9.2）
- **HUDは3つだけ。** スコア（1000への進捗）／ NEXT ／ 経過時間。消去レイヤー数は出さない（スコアと同じ情報になる）
- 影の光は必ず真上。斜めにするとピースの高さでXZ位置がずれて役に立たない
- 影は `LandingSolver` の結果から描く投影メッシュ。実ライトの影は使わない
- 足す必要が出たときは、プレイ空間の中ではなく境界面（床・壁）に置く

## 見た目

- ブロックは角丸・柔らかい質感。着地スクワッシュや揺れは `transform.localScale` とマテリアルの操作のみで表現する
- **見た目の変更がロジックに影響してはいけない。** 演出を全部止めてもゲームの結果は同じ
- 物理エンジン（Rigidbody / ソフトボディ）は使わない。決定性が失われ、EditModeテストが書けなくなる

## 変更したら

- `Core` を触ったら EditMode テストを通す
- 仕様に関わる変更をしたら `Docs/3DTetris_GameDesign.md` を同じコミットで更新する
- 仕様書の「14. 作らないもの」に載っている機能は、明示的に依頼されない限り実装しない
